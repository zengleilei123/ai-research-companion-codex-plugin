interface:
  display_name: "Research Project Onboarding"
  short_description: "Set up a new research workspace with only the essential questions."
  default_prompt: "Use $project-onboarding to set up this research project, ask at most three key questions, then update the project state."
policy:
  allow_implicit_invocation: true
