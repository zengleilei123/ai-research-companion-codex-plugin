interface:
  display_name: "Literature Research"
  short_description: "Survey papers, repos, baselines, and novelty risk for a research idea."
  default_prompt: "Use $literature-research to check local references first, identify gaps, and recommend the strongest baseline."
policy:
  allow_implicit_invocation: true
