interface:
  display_name: "Experiment Memory Scout"
  short_description: "Find prior experiments, weekly notes, and references before planning new work."
  default_prompt: "Use $experiment-memory-scout before planning this experiment to scan prior project memory for related evidence and risks."
policy:
  allow_implicit_invocation: true
