interface:
  display_name: "Research Change Memory"
  short_description: "Record what changed and why."
  default_prompt: "Use $change-memory to record what changed, why it changed, evidence, and next actions."
policy:
  allow_implicit_invocation: true
